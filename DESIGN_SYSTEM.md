# سند مرجع سیستم طراحی (Design Tokens)

> منبع: کپی دقیق زبان بصری اپلیکیشن رفرنسی که بنیان‌گذار عکس‌هایش را ارسال کرد (رنگ‌ها، فرم‌ها، فاصله‌گذاری، سایه‌ها، تایپوگرافی و کامپوننت‌ها) — **بدون هیچ توجهی به محتوای آن اپ**. سبک بصری: نئوبروتالیستِ سالم/فیتنسی — پس‌زمینهٔ سفید، حاشیه‌های ضخیم مشکی، اشکال کپسولی (pill)، سایه‌های افست تخت (بدون بلر)، تایپوگرافی درشت و بولد، برچسب‌های حروف‌بزرگ با letter-spacing.
>
> تمام صفحات این پروژه از این توکن‌ها پیروی می‌کنند، فارسی و RTL هستند.

پیاده‌سازی: `app/globals.css` (توکن‌های CSS/Tailwind v4 `@theme`) + `app/fonts.css` (فونت).

---

## ۱. رنگ (Color)

| نام توکن | هگز | استفاده |
|---|---|---|
| `paper` | `#FFFFFF` | پس‌زمینهٔ اصلی صفحات |
| `ink` | `#121212` | متن اصلی، حاشیهٔ کامپوننت‌ها، دکمهٔ اصلی (پیش‌فرض مشکی) |
| `ink-black` | `#000000` | سایهٔ افست تخت (hard shadow) |
| `surface` | `#F4F4F5` | پس‌زمینهٔ اینپوت‌ها/کارت‌های ثانویه |
| `surface-strong` | `#ECEDED` | پس‌زمینهٔ کارت روی surface (تودرتو) |
| `track` | `#E7E7EA` | ریل نوار پیشرفت، اسکلتون |
| `muted` | `#9A9AA1` | متن ثانویه / کپشن خاکستری |
| `muted-strong` | `#6F6F76` | متن ثانویهٔ پررنگ‌تر |
| `faded` | `#C9C9CE` | متن/دکمهٔ غیرفعال (disabled) |
| `border` | `#121212` | حاشیهٔ استاندارد کامپوننت‌ها (۲px) |
| `border-soft` | `#DCDCE0` | حاشیهٔ ملایم (خطوط جداکننده) |
| `accent-blue` | `#1F63F2` | رنگ تأکیدی ۱ — CTA فعال، آیکن/تگ نوع اول |
| `accent-blue-soft` | `#E4EDFF` | پس‌زمینهٔ ملایم تأکید آبی (چیپ آیکن، کارت اطلاعاتی) |
| `accent-pink` | `#E31C5F` | رنگ تأکیدی ۲ — اکشن‌های قاطع/پایان، آیکن/تگ نوع دوم |
| `accent-pink-soft` | `#FCE3EC` | پس‌زمینهٔ ملایم صورتی |
| `accent-gold` | `#D9A441` | رنگ تأکیدی ۳ — دستاورد/پیشرفت/برجسته‌سازی |
| `accent-gold-soft` | `#F7ECD3` | پس‌زمینهٔ ملایم طلایی |
| `success` | `#2FA84F` | وضعیت موفقیت (مثلاً +۱ روی استریک) |
| `success-soft` | `#E1F5E6` | پس‌زمینهٔ ملایم سبز |

قاعده: هر رنگ تأکیدی همیشه یک نسخهٔ «soft» به‌عنوان پس‌زمینهٔ چیپ/آیکن دارد؛ خود رنگ فقط روی متن/آیکن/دکمه با کنتراست بالا می‌آید.

## ۲. تایپوگرافی

فونت: **IRANYekan (FaNum)** در ۵ وزن — Light 300 / Regular 400 / Medium 500 / Bold 700 / ExtraBold 800. پشتیبانی کامل RTL و اعداد فارسی.

| نقش | اندازه | ارتفاع خط | وزن | کاربرد |
|---|---|---|---|---|
| `display` | 32px | 38px | ExtraBold 800 | تیتر صفحات آنبوردینگ/مارکتینگ |
| `h1` | 26px | 33px | ExtraBold 800 | عنوان سؤال/صفحه |
| `h2` | 20px | 26px | Bold 700 | عنوان بخش/کارت |
| `body` | 16px | 24px | Regular 400 | متن اصلی |
| `body-sm` | 14px | 21px | Regular 400 | متن ثانویه |
| `caption` | 12px | 16px | Medium 500، حروف‌بزرگ، letter-spacing 0.06em | برچسب/راهنما (مثل «SESSIONS»، «THIS WEEK») |
| `stat` | 52px | 52px | ExtraBold 800 | عدد شاخص بزرگ (مثل شمارندهٔ ست/رپ) |
| دکمه | 15px | — | Bold 700، حروف‌بزرگ (فارسی: بدون uppercase، فقط bold)، letter-spacing 0.02em | متن دکمه‌ها |

## ۳. فاصله‌گذاری و Grid

پایه: **۴px**. اسکیل استاندارد: 4, 8, 12, 16, 20, 24, 32, 40, 48, 64.

- پدینگ افقی صفحه: `24px` (`px-6`)
- فاصلهٔ بین بلوک‌های اصلی: `24px`–`32px`
- فاصلهٔ بین دکمه‌های پشت‌سرهم در فرم: `12px`–`16px`
- ارتفاع دکمهٔ اصلی/اینپوت: `56px`
- ارتفاع دکمهٔ کوچک/چیپ: `40px`–`44px`
- پدینگ داخلی کارت: `20px`–`24px`

Breakpoints (موبایل‌فرست): `base` تا 480px (طراحی اصلی)، `sm` 480px+، `md` 768px+ (تبلت) — کانتینر حداکثر عرض `480px` روی صفحات بزرگ‌تر برای حفظ حس اپ موبایل.

## ۴. شعاع گوشه (Radius)

| توکن | مقدار | کاربرد |
|---|---|---|
| `radius-pill` | 999px | دکمه‌ها، اینپوت‌ها، چیپ‌ها، نوار پیشرفت |
| `radius-lg` | 28px | کارت‌های اصلی (کارت رویداد، کارت پروفایل، مودال) |
| `radius-md` | 20px | کارت‌های ثانویه، جعبه‌های آماری |
| `radius-sm` | 14px | آیکون‌چیپ، عناصر کوچک |

## ۵. سایه (Shadow) — امضای بصری اصلی

سایه‌ها **تخت و بدون بلر** هستند (افست ساده)، نه سایهٔ نرم معمول:

| توکن | مقدار |
|---|---|
| `shadow-hard-sm` | `-3px 3px 0 0 #000000` |
| `shadow-hard` | `-4px 4px 0 0 #000000` |
| `shadow-hard-lg` | `-6px 6px 0 0 #000000` |

قاعده: تمام دکمه‌ها و کارت‌های سطح اول `border-2 border-ink` + `shadow-hard` دارند. جهت افست برای هماهنگی با RTL آینه شده (افست به سمت چپ-پایین به‌جای راست-پایین همانند نسخهٔ رفرنس).

## ۶. حاشیه (Border)

استاندارد: `2px solid var(--color-border)` روی اکثر کامپوننت‌ها (دکمه، اینپوت، کارت، چیپ). این ضخامت حاشیه بخشی از هویت بصری است و نباید نازک‌تر شود.

## ۷. کامپوننت‌های پایه و state ها

پیاده‌سازی در `components/ui/`:

- **Button** — variants: `primary` (پس‌زمینهٔ ink، متن سفید)، `accent-blue`، `accent-pink`، `secondary` (پس‌زمینهٔ سفید، حاشیهٔ مشکی)، `outline` (بدون سایه/پس‌زمینه، فقط حاشیه — برای اکشن‌های ثانویهٔ کوچک). state: `default` (با shadow-hard) / `pressed` (بدون شادو، جابه‌جایی ۲px به‌سمت شادو) / `disabled` (پس‌زمینهٔ faded، بدون شادو، بدون حاشیهٔ پررنگ).
- **Input** — state: `default` (پس‌زمینهٔ surface، بدون حاشیهٔ پررنگ) / `focus` (حاشیهٔ ۲px ink) / `error` (حاشیهٔ accent-pink + متن خطا زیر آن).
- **Card** — دو نوع: کارت رویداد (تصویر/آیکون بالا، عنوان، متادیتا) و کارت پروفایل (آواتار، نام، تگ‌ها). همیشه `radius-lg` + `border-2` + `shadow-hard`.
- **Badge** — `verified` (پس‌زمینهٔ ink، آیکون تیک سفید، pill کوچک).
- **Avatar** — دایره‌ای، حاشیهٔ ۲px مشکی اختیاری برای حالت برجسته.
- **Chip/Selector** — حالت‌های multi/single select: `unselected` (سفید، حاشیهٔ مشکی) / `selected` (پس‌زمینهٔ ink، متن سفید).
- **Segmented control** — دو یا چند گزینه (مثل IMPERIAL/METRIC)، انتخاب‌شده pill مشکی توپر.
- **Progress bar** — ریل `track` + پرشدهٔ `ink` یا رنگ تأکیدی، `radius-pill`.
- **Stat box** — جعبهٔ حاشیه‌دار برای نمایش عدد+واحد (مثل مدت‌زمان/سختی).

## ۸. الزام RTL

- `<html dir="rtl" lang="fa">` در ریشهٔ اپ.
- تمام آیکون‌های جهت‌دار (فلش برگشت، شورون) باید mirror شوند در RTL؛ از `rtl:scale-x-[-1]` یا آیکون‌های مخصوص RTL استفاده می‌شود.
- چیدمان فلکس/گرید با منطق منطقی (`ps-`, `pe-`, `text-start` به‌جای `text-left/right`) نوشته می‌شود، نه چپ‌به‌راست معکوس‌شده.
- اعداد با فونت FaNum (ارقام فارسی) نمایش داده می‌شوند.

---

**وضعیت:** پیش‌نویس اول — در انتظار تأیید بنیان‌گذار قبل از شروع ساخت صفحات اصلی اپ.
